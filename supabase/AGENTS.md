<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# supabase — DB · RLS · Edge Functions (source of truth)

> Read root [`CLAUDE.md`](../CLAUDE.md) first — the quickstart; Notion holds the deep docs. Package-specific rules only below.

- **New here?** The system map is **Notion 📚 Docs**; read the page your footprint touches before touching its EFs (I-8). Surfaces: **Apps** · the place profile: **Atlas** · the pipeline: **Intake** · money: **Checkout**, **Rewards**, **Visits** · the Reservationist (fleet, lines, legs, timing): **Reservations**. Auth doors and the money laws: **Rules §2**.
- Run every `supabase` command from **this package** — the CLI config is `supabase/config.toml` inside it. All Supabase files live only here; kill stray `supabase/` config stubs elsewhere in the monorepo, since a stub links against a divergent migration ledger.
- **Package layout:** `supabase/functions/` (EFs + `_shared/`) · `supabase/migrations/` · `integrations/twilio/` (`numbers.json` = number inventory of record + TwiML) · `integrations/elevenlabs/` (fleet spec lives in code: `_shared/reservationist-fleet.ts` / `-kb.ts`) · `scripts/` (`deploy.sh` = db push + regen types; `sync-root-env.sh`; `setup-twilio-*.sh` apply `numbers.json`; `linklab/` link-discovery benchmark, run instructions in `scripts/linklab/run.ts`).
- **The EF name is the ACL:** `actor-origin-verb-noun`, exactly one caller per endpoint from the closed set. `_shared/` holds shared code (internal naming free-form). Only `supabase-edgefunc-*` endpoints accept the internal caller; the origin propagates via `X-Internal-Caller`.
- **Mirror + verify:** every cloud change (schema/RLS/EF) mirrors into this package the same session. After any EF deploy confirm cloud == repo (`get_edge_function`).
- **Migrations:** MCP `apply_migration` stamps its own server-side timestamp ≠ the repo filename — reconcile `schema_migrations` after, or the next `db push` re-runs those files. Prod EF deploys and security-sensitive DDL are gated by the harness classifier: attempt a `supabase …` command once cleanly, else hand off a `deploy:` step.
- **`profiles` is `SECURITY INVOKER`.** Consumer browse (`consumer-web-list-places`) reads it with the ANON key and relies on RLS as the single source of truth, so the view MUST run as the caller: `places_select_public_visible` gates it to `state ∈ {active,lead}` + `content_state ∈ {ready,generating,queued,failed}`; service-role EFs bypass RLS and see every row. **INVARIANT: any `create or replace view public.profiles` MUST include `with (security_invoker = true)`** — dropping it silently reopens the leak. `rls_enabled_no_policy` tables are the deliberate EF-only lockdown; adding policies OPENS access. Client roles hold no privileges on EF-only tables and SELECT-only on browse/vocabulary tables.
- **The bill engine is ADDITIVE, and CONTEXT cuts first.** `promos_config.v12` = `{ version: 12, visits, orders, cap }` (older blobs migrate on read; stale SAVEs 409). `resolveTicketRate` pays `visits.base(strategy × class) + welcome + each earned bonus` (**no per-class override**), clamped to 100, on the first cap-pesos. **The plan prices nothing.** **`orders` is PARKED**, one scalar per strategy. Best-of over the v13 grid keys is the no-config fallback. Welcome grants on the first verified ticket. `v12.cap` (200/500/1000) is the fallback; the per-place knob is `places.monthly_promo_cap`. Strategies `zero`/`conservative`/`aggressive`/`dominant` — ONE governs both ladders.
- **Class and plan are two axes; only CLASS is priced.** `consumers.class_key` is a metal (`bronze` ‹ `silver` ‹ `gold` ‹ `diamond`); `consumers.plan` (`free`|`premium`) buys perks, never a rate. `identityForClassKey` returns the class alone, bridging legacy keys (`premium`→bronze); plan-for-perks lives in `_shared/membership.ts`.
- **`place_profiles.name` is a GENERATED column** — `coalesce(mesita_name, google_name)`. The identity spine is the immutable `google_place_id`; `google_name` is the Intaker's cached observation, `mesita_name` the operator override (NULL ⇒ follow Google). **No EF may write `name`** — Postgres rejects it (428C9) and `_shared/place-name-writes.test.ts` guards it.
- Realtime is for ticket workflows only. **`admin_reset_database` discovers every `public` base table at run time and truncates all except rows in `public.admin_reset_preserve`** — a new operational table is wiped by default; a table that must SURVIVE is an `INSERT` into that registry, never a copy-paste into the function body. Required survivors (`app_config`, `super_admins`, vocabularies) are asserted before truncate, so an emptied registry aborts instead of wiping blind. Storage is purged over the Storage API, never by deleting `storage.objects` rows.
- **Intaker ops (process, not agent):** cron EF pipeline `supabase-cron-enrich-place-{research,analysis,contents}` over `place_research`. Canonical stage order is code (`enrich-pipeline.ts`): `research → analysis → contents → done`. Drivers: pg_cron job `run-place-enrichment-stages` plus `admin-web-enrich-place` modes `full|analysis|contents` — prefer cheap modes when `gathered`/`analysis` exist, since `full` burns Apify/Perplexity/Firecrawl. Judge by DB effects (`gathered->sources`, places fields, media), never green beacons. Spend kill switch: `select cron.unschedule('run-place-enrichment-stages');`.
- CI: `supabase.yml` — deno lint · test · check · pgTAP (`supabase/tests/database/`, migrations replayed), path-filtered to `supabase/**`. Instruction-file sync = root `rules.yml`.
