# Doctor — 2026-08-09

**Verdict:** DEGRADED (4 P1)
**Scopes run:** 1–7 partial (skipped: 1.2 full EF body hash for all 138, 1.3 file↔ledger pairwise beyond counts, 2.5–2.7, 3.6–3.8, 3.9 quotas, 6.2–6.8 typecheck/Vercel/CI, 8 ledger — Linear MCP `needsAuth`)
**origin/main:** `485f1bd4216ba57e4abe9295c454691c7dc58181`
**Mode:** BASELINE (no prior `docs/doctor/` report on origin/main)

## Diff vs (none)

- NEW: all findings below (BASELINE)
- PERSISTING: n/a
- RESOLVED: n/a

## Findings

### [P1] 4.4 / 3.4 — `CLASS_SEGMENTS` order ≠ live `classes.rank`
- live: `standard=0`, `influencer=1`, `premium=2`, `aura=3` (`SELECT key, rank FROM classes ORDER BY rank`)
- repo: `supabase/supabase/functions/_shared/rewards-config.ts:25-30` orders `standard, premium, influencer, aura` while commenting “rank order”
- evidence: re-ran SQL 2026-08-09; `git show origin/main:…/rewards-config.ts`
- fix: reorder `CLASS_SEGMENTS` to match `classes.rank` (and any admin UI that iterates the array as a ladder)
- blast radius: any code that treats array index as class rank / ladder display
- linear: SKIPPED (Linear MCP unauthenticated this session)

### [P1] 4.3 / 4.4 — Lineup `sm.what.tol` saved but ignored
- admin: Lineup Config exposes/saves `sm.what.tol` (`apps/web-admin` lineup SmBox)
- serving: `supabase/supabase/functions/_shared/lineup-rank.ts:95` hardcodes `whatRel: "none"` with comment “filters not on the EF yet”
- evidence: `git show origin/main:supabase/supabase/functions/_shared/lineup-rank.ts` around L85–100; live `scoring_config.v = 12`
- fix: wire `whatRel` from config **or** remove/hide the admin knob and mark staged
- blast radius: Lineup “what” subscore always 1 — operator thinks they tune relevance
- linear: SKIPPED (auth)

### [P1] 5.x / Product Rules — blended-rate privacy leak surface
- Product Rules: businesses/staff must NEVER learn consumer class/door
- repo: `business-web-list-tickets/index.ts:45` selects `consumer:consumers(…, class_key, class_origin)`
- evidence: `git show origin/main:supabase/supabase/functions/business-web-list-tickets/index.ts` L45
- fix: drop `class_key`/`class_origin` from business ticket payloads (keep resolve inside `_shared`)
- blast radius: any business console ticket list that surfaces or caches those fields
- linear: SKIPPED (auth)

### [P1] 4.x / E — Memo product surface parked while engine live
- Notion Product Rules §E / §A: Memo is a live product agent (`consumer-web-ask-memo`)
- consumer UI: `HomeModeNav.tsx` Memo tab `soon: true`; `/home/ai` redirects to swipe
- admin: Memo Config + Playground live; greeting/instructions enforced on ask-memo path
- evidence: `git show origin/main:apps/web-consumer/src/components/consumer/home/HomeModeNav.tsx` L30–46; MemoLayoutShell notes “Home Ask AI is parked”
- fix: unpark Memo on web+mobile **or** demote Product Rules wording to staged
- blast radius: consumer discovery IA vs operator-tuned persona
- linear: SKIPPED (auth)

### [P2] 2.1 — SECURITY DEFINER RPCs executable by `authenticated`
- advisor WARN: `public.is_project_member(uuid)`, `public.is_super_admin()` callable via `/rest/v1/rpc/…`
- evidence: `get_advisors(type=security)` 2026-08-09
- fix: revoke EXECUTE from `authenticated`/`anon` if EF-only (or document intentional)
- blast radius: info disclosure / privilege probe surface
- remediation: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable

### [P2] 2.1 — Leaked password protection disabled
- advisor WARN: Auth leaked-password check off
- evidence: `get_advisors(type=security)` cache_key `auth_leaked_password_protection`
- note: consumer auth is phone OTP; still a dashboard posture finding
- remediation: https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

### [P2] 4.3 — Memo staged knobs still persisted
- `memo_provider`, `memo_web_grounding`, `memo_perplexity_model` written by admin update EF; serving path uses `models_config.memo.*`
- evidence: `MemoConfigClient.tsx` header (Verified 2026-08-09) chips `not-wired` / fallback; `supabase-edgefunc-get-memo-config` reads models blob
- fix: remove UI fields **or** wire them; keep chips truthful (currently truthful → P2 debt, not silent bypass)

### [P2] 4.3 — `agents_config` live, no admin page
- live `app_settings.agents_config.agents.a1–a4` + `knowledgeDocIds` present; Reservationist sync/call EFs read them
- no `/agents-config` (or equivalent) under web-admin
- fix: admin surface **or** document as code-managed-only in Product Rules binding list

### [P2] 5.1 — Notion Rules caller table lags ARCHITECTURE.md
- Notion Product Rules still lists active caller `reservationist-agent` and omits `check-web` / `eleven-a*`
- `supabase/ARCHITECTURE.md` on origin/main correctly documents `check-web` + `eleven-a1…a4` and states there is no `reservationist-agent` folder prefix
- fix: mirror ARCHITECTURE closed-set into Notion Product Rules §A same session

### [P2] 2.2 — Unindexed FK `consumers.invitation_class_key`
- advisor: `consumers_invitation_class_key_fkey` without covering index
- evidence: `get_advisors(type=performance)`

### [P2] 2.2 — Many unused indexes (INFO)
- including `places_embedding_hnsw`, several membership_strikes/coupons/tickets indexes
- evidence: performance advisors list 2026-08-09 — hygiene only; do not drop without query proof

### [P2] 1.x docs — EF census comments stale in Doctor skill / README
- live+repo EF count: **138** callable (match 1:1)
- Doctor skill text still says “124”; some README lines still under-count
- not a cloud/repo drift

## Scope ledger

| Scope | Checks | OK | Findings | Skipped |
| --- | ---: | ---: | ---: | ---: |
| 1 Backend congruence | 1.1, 1.5–1.6 | 3 | 0 P0 | 1.2 full body, 1.3 pairwise, 1.4 full schema reconstruct |
| 2 Postgres health | 2.1–2.2 | advisors run | 2 WARN + unused indexes | 2.3–2.9 deep |
| 3 Data integrity | 3.1–3.4, 3.10 | orphans 0, bad class/plan 0, stuck 0, cats 101, tags 200 | CLASS_SEGMENTS (see P1) | 3.5–3.9 |
| 4 Config enforcement | memo/lineup/sourcing/models/rewards spot | scoring v12, sourcing fallbacks aligned on main | Lineup whatTol; Memo parked; staged knobs | full knob grep all pages |
| 5 Callers/boundaries | inventory + privacy select | EF prefixes healthy | class leak select; Notion caller lag | full `.from(` grep |
| 6 Repo/IT | — | — | — | SKIPPED (budget) |
| 7 Runtime | EF logs sample | cron retries 200s | 1× `admin-web-check-api-health` 401 | full 24h error signature diff |
| 8 Ledger | — | — | — | SKIPPED (Linear `needsAuth`) |

## Metrics (trendable)

- DB size: **45 MB**
- Largest tables (total relation): app_settings 136 kB · place_tags 104 kB · tickets 104 kB · classes 80 kB · place_categories 80 kB
- EF count vs deploy: **138 cloud / 138 repo / 0 ghosts / 0 missing**
- Deploy-drift count (inventory): **0**
- Advisor security: **2 WARN** + 29 intentional `rls_enabled_no_policy` INFO (EF-only lockdown)
- Advisor performance: **1 unindexed FK** + **24 unused index** INFO
- Orphan projects/places: **0 / 0**
- Dup `google_place_id`: **0**
- Bad class / bad plan rows: **0 / 0**
- Categories / tags: **101 / 200**
- `reward_rules` rows: **40**
- Stuck `place_research` (>2h running/pending/claimed): **0**
- `scoring_config.v`: **12**
- `rewards_config.v10.version`: **10**
- `models_config.v`: **1**
- `reservations_config.priority`: `["phone"]`
- `sourcing.consumer_search.minRating` live: **1**
- Dead-knob count (this run, explicit): **≥4** (lineup whatTol; memo provider/webGrounding/perplexityModel; agents_config no UI)
- Linear filing: **BLOCKED** (MCP needsAuth)

## Assumptions

- Audited **live cloud** vs **`origin/main`**, not the cloud agent’s behind checkout.
- `rls_enabled_no_policy` INFO tables treated as intentional EF-only lockdown (Product Rules), not findings.
- Memo greeting wiring on main is **OK** (ask-memo bootstrap + client fallback); admin chips match.
- Sourcing admin/EF fallback `consumer_search.minRating=1` aligned on main with live row (older drift resolved).
