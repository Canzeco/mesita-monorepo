# Test plan — MESITA-578 mobile web-identical redesign

Branch: `cursor/autoplan-mesita-578-system-2823`  
Source plan: `docs/plans/MESITA-578-web-identical-redesign.md`  
Generated by: `/autoplan` eng review

## CI gates (every slice PR)

- [ ] `pnpm typecheck` in `apps/mobile-consumer`
- [ ] `pnpm lint`
- [ ] `npx expo export --platform web`
- [ ] `:8081` visual glance (launch.json `mobile-consumer`)

## Per-slice checklist

### MESITA-583 — Me + kit
- [ ] `@rn-primitives/{portal,switch,slot}` installed; PortalHost in root layout
- [ ] One sheet opens/closes on web export before bulk Me migration
- [ ] Me order matches web: Identity → IG → Class → Personal → Settings → Share → AI → Contact → Sign out
- [ ] No "Me" H1; no Paper Chip on hero
- [ ] Soon rows inert + Soon pill (no Alert)
- [ ] Class box visible without subscribe CTA
- [ ] Personal disabled when `!profile`; save shows loading/error
- [ ] Settings switches persist
- [ ] Keyboard does not cover sheet fields
- [ ] Android back dismisses sheet
- [ ] CLAUDE.md no longer mandates Paper; AGENTS.md synced

### MESITA-580 — Rewards + Reservations
- [ ] Coral MyQrCard DNA (gradients, QR plate ink `#2b1233`) or documented mock QR
- [ ] Copy code affordance works when code present
- [ ] Zero-stats / no-code states don't crash
- [ ] Reservations: Upcoming/History segment + pink-gradient empty (not Paper Card)
- [ ] No Stripe/subscribe links

### MESITA-581 — Tab bar + shell
- [ ] Custom tab bar: hairline + icon ring (not Home pink pill)
- [ ] Dynamic `Me · Free|Premium` (null class → Free)
- [ ] MesitaMark Home tab
- [ ] Soon intercept rule documented and tested
- [ ] Home/Me gradient backgrounds; hero radial blob restored
- [ ] Fraunces 600+700 loaded; no infinite splash
- [ ] Bottom safe area / home indicator OK on device note

### MESITA-582 — Auth + Search + remove Paper
- [ ] sign-in + onboard off Paper; OTP/onboard errors still shown
- [ ] GooglePlaceSheet + SearchResultsPanel off Paper
- [ ] Grep clean: no `react-native-paper`, no `PaperProvider`, no `paper-theme`
- [ ] CI grep or eslint ban on `react-native-paper`
- [ ] Notion Product/Dev rule mirrored
- [ ] A11y: roles/labels on new primitives; ≥44px targets
- [ ] Memo + Social still live on Home (regression)

## Friday-night regressions (must not happen)

1. PaperProvider removed while a Paper Portal remains → blank overlays
2. Custom tab bar without bottom inset → clipped `Me · Premium`
3. PortalHost missing → sheets dead on device
4. Auth TextInput without keyboard avoid → OTP under keyboard
5. Font gate too strict → splash forever
6. Class box grows a subscribe button → Apple risk

## Human-only

- EAS / TestFlight: Portal shadows, tab bar, true device fonts
