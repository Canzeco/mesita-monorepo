<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-check — Mesita Validate's legacy duplicate, live at check.mesita.ai

> Read root [`CLAUDE.md`](../../CLAUDE.md) first — the quickstart; Notion holds the deep docs. Package-specific rules only below.

- **This directory is the legacy duplicate of `apps/web-validate`**, which is canonical — ship product changes there first and mirror them here only to keep production matching. `apps/web-check` still exists because the live Vercel project `mesita-web-check` deploys from here today, serving `check.mesita.ai` in production. Once that project's Root Directory retargets to `apps/web-validate` (or DNS moves to `validate.mesita.ai`, needs-human), this directory retires.
- **The staff side of THE TICKET v4 (MESITA-1084/1090/1092):** the waiter scans the QR a guest generated and lands on `/<code>` — bill, tip, reward and proof at a glance. **Scan · glance · approve — two touches**: one `Aprobar` (freezes the amount; every verdict carries `expectedUpdatedAt`, and a stale render disables it), one `Pedir corrección` with exactly three chips (`bill` · `proof` · `reward`) — the guest fixes it live on the same code, no new QR. Second touch: `Pago recibido` validates and closes. **Staff never judge a TASK** — story/review rows are informational; the ticket-level verdict is the floor's whole job. Unbilled (v3) tickets keep the legacy bill+close path until MESITA-1093. Root page = explainer + hand-typed code fallback; legacy `/check/<code>` redirects.
- **Proof screenshots don't change the staff job** (MESITA-1030): submissions self-verify, and `shapeCheckPayload` only ships `story.screenshot_url` for pre-v3 leftovers. **Never render the proof image here** — reading it is **Ojo**'s job (admin Visits; engine MESITA-1034). Don't grow a per-task verdict, a free-text rejection, or a fourth fix reason.
- **The product name is Validate** (MESITA-1114); EFs are `validate-web-*` (MESITA-1116) — the same backend `apps/web-validate` calls, since the Supabase project is a singleton. `check_code` and `CHECK_URL_BASE` still mint `https://check.mesita.ai/` until DNS for `validate.mesita.ai` (needs-human). `checkout` still means **Stripe**. Card-through-Mesita is retired.
- **No login by design.** The 128-bit check code in the URL is the whole authentication; the `validate-web-*` Edge Functions are `verify_jwt=false`. Deliberately **no supabase-js dependency** — `src/lib/check-api.ts` is plain fetch (public constant URL; `NEXT_PUBLIC_SUPABASE_URL` optional override). Keep it that way.
- **Staff-facing → Spanish (es-MX) copy**, `robots: noindex`, light theme.
- **Deploys via the Vercel `mesita-web-check` project** (created 2026-08-03), pointed at `Canzeco/mesita-monorepo`, Root Directory `apps/web-check` today. Root Directory must become `apps/web-validate` (needs-human); `check.mesita.ai` stays attached until `validate.mesita.ai` is bound.
- **Live QR base:** `CHECK_URL_BASE` = `https://check.mesita.ai/` (MESITA-814). Old `mesita.ai/check/<code>` QRs permanently redirect via web-landing.
- "venue" is prohibited in copy → "place" / "business" (same rule as web-landing).
- CI: `web-check.yml` — lint · typecheck · build (Node 22+), path-filtered to `apps/web-check/**`.
