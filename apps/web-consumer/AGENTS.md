<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-consumer — consumer app (consumer.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first — the quickstart; Notion holds the deep docs. Package-specific rules only below.

- **Light theme + semantic tokens only** (`bg-card`, `text-foreground`, `border-border`) — never `bg-zinc-900`/`text-white`. Surfaces read **premium**: branded gradients, tinted icon circles, differentiated chips; wireframe stacks = regression. Design map: Notion Docs › Design.
- **Overlay & loading primitives are mandatory:** route modals = `SlideOverShell`/`BottomSheetShell` from the segment `layout.tsx` (never `page.tsx`); state overlays = `LocalSheet`/`LocalDialog` (never `fixed inset-0`); loading = `Spinner`/`Skeleton`. Z: BottomNav 40 · @modal 120 · local 130 · Toaster 140 · RouteBadge 150. Lint baseline **0**.
- **Parked blocks are not dead code** — look for a parking comment, a coming-soon route, or a live `@modal` before deleting. Un-parking Catalog = `soon: false` plus the page body.
- **Five bottom tabs — Home · Search · Pay · Inbox · Me**, plain labels. Routes + legacy redirects: `src/lib/consumer-route-contract.ts`, drift-guarded against mobile.
- **`(shell)/layout.tsx`'s `getUser()` IS the auth wall**, inherited segment-wide; `PROTECTED_PREFIXES` = edge fast path only. `route-structure.test.tsx` guards the shape: every page under `(shell)`; every `@modal` intercept in `isModalContractPath` (**miss it → BLANK modal**) with a hard twin; no dangling redirect; one tab lit per surface.
- **The third tab is LABELLED Pay** (QR icon); **`/new-visit`** creates one, **`/visit/{id}`** is one. **A rename stops at the label** — never cascade into routes, code or columns: object = ticket, column = `kind`, Inbox section = Visits. `/rewards*`, `/pay*`, `/qr`, `/ticket/:id` 308 forward. **`/visit/{id}` lights INBOX**; `BottomNav` matches `matchPrefixes: string[]`. `checkout` means Stripe.
- **Inbox = one container, four sections, fixed order: Visits · Orders · Reservations · Notifications** (bare `/inbox` → visits; **order load-bearing**, pinned by a test; tray icon). **Visits** reads the wallet's `useConsumerTickets`. **Orders** parked: no table, EF or type. **Reservations** = ONE feed, upcoming soonest-first then past most-recent-first. **Notifications = YOUR activity** — global lives at Home › Social. **Detail stays at `/reservation/[id]`**.
- **The wallet is a searchbar over a bare place list** — no tabs, no ticket lists, **no steps rail**; keep the "Your visits live in Inbox" link. Header = `search/SearchBar`; `place-list-filter` matches name · zone · category. One tap creates the ticket at `base` and opens **THE TICKET** at `/visit/[id]` — **SEVEN steps (v4): Bill · Reward · Task · QR · Pay · Validate · Results** (`lib/ticket-journey.ts` = the tested machine). Bill+tip is guest-entered, **required on the QR**; tip rides the pre-discount subtotal, never capped. Staff approve (freezes the amount) or send back ONE named fix — amber, same code, no new QR. Pay = at the place (card + Yums staged); live sync = `consumer-web-get-ticket` at 10s. Rates come from `consumer-web-get-discount-quote`, never a static ladder.
- **Proofs are screenshots** → `ticket-proofs` bucket (RLS: own folder), URL persisted. The declaration verifies; nothing reads the image yet (Ojo, staged).
- **Home hub = five modes; only Swipe and Favorites are FUNCTIONAL.** Catalog · Memo · Social redirect to swipe; the pill opens a coming-soon dialog. Five pills fit `max-w-md` at rest; `HomeModeNav` scrolls only as the large-text fallback. The AI mode is **`/home/chat`** (`/home/ai` 308s; the `?mode=` map holds a bare segment — grep misses it). **Don Memo** is its persona, Spanish-first; app chrome stays English.
- **Classes v2 — two axes, never merged.** `bronze ‹ silver ‹ gold ‹ diamond` earned/public; `free`/`premium` paid/private; LEGACY keys bridge in `class-context`. Me is flat; Class + Settings are modals. Favorites = localStorage. Referral = `/share`.
- Edge Functions: `consumer-web-*`.
- CI: `web-consumer.yml` — lint · typecheck · test · build, all blocking (Node 22+), path-filtered.
