<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-consumer — consumer app (consumer.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first — the quickstart; Notion holds the deep docs. Package-specific rules only below.

- **Light theme + semantic tokens only** (`bg-card`/`text-foreground`/`border-border`) — never `bg-zinc-900`/`text-white`. Surfaces read **premium**; wireframe stacks = regression. Map: Notion Docs › Design.
- **Overlay & loading primitives are mandatory:** route modals = `SlideOverShell`/`BottomSheetShell` from the segment `layout.tsx`, never `page.tsx`; state overlays = `LocalSheet`/`LocalDialog`, never `fixed inset-0`; loading = `Spinner`/`Skeleton`. **ONE semi-modal: `SEMI_MODAL_PANEL_CLASS` (80% of the card) on BOTH sheet shells** — never hardcode a panel height. **A confirm is a `LocalDialog`, not a short sheet.** Z: BottomNav 40 · @modal 120 · local 130 · Toaster 140 · RouteBadge 150. Lint baseline **0**.
- **Parked blocks are not dead code** — check for a parking comment, coming-soon route, or live `@modal` before deleting. Un-park = `soon: false` + the page body.
- **Four bottom tabs — Discover · Pay · Activity · Me**, plain labels; `/place` lights Discover. Routes: `consumer-route-contract.ts`. **Mobile mirror = SEPARATE PR.**
- **Discover = `/discover`, FIVE modes, segments match labels** (the exception): map · search · swipe · chat · favs. Only map + search live; three parked, bodies on disk. **Search** absorbed Name+Catalog+Social — `CatalogRails`/`SocialFeed` mount INTO it, they do not get routes back. Default `/discover/map`, pinned never parked. `/home*`/`/explore*`/`/search` 308 there in ONE hop (T4 caps 2).
- **Two section-nav looks IS the rule:** equal columns when they fit (Activity); a content-width **scrolling rail** when they cannot. Discover keeps the rail at five: "Search" needs ~68px against 68.6px per equal column — 0.6px is inside the measurement error. Parked 55%. A SIXTH pushes pills off-screen; re-measure first.
- **`(shell)/layout.tsx`'s `getUser()` IS the auth wall**, inherited segment-wide; `PROTECTED_PREFIXES` = edge fast path only. `route-structure.test.tsx` pins: every page under `(shell)`; every `@modal` intercept in `isModalContractPath` (**miss it → BLANK modal**) + hard twin; no dangling redirect; one tab lit.
- **The third tab is LABELLED Pay** (QR icon); **`/new-visit`** creates one, **`/visit/{id}`** is one. **A rename stops at the label** — never cascade into routes, code or columns: object = ticket, column = `kind`. `/rewards*`, `/pay*`, `/qr`, `/ticket/:id` 308 forward. **`/visit/{id}` lights ACTIVITY**; `BottomNav` matches `matchPrefixes: string[]`. `checkout` means Stripe.
- **Activity = one container, five sections, fixed order: Wallet · Visits · Orders · Bookings · Alerts** (bare `/inbox` → visits: **default ≠ first, deliberately**; **order load-bearing**, pinned; tray icon). Wallet/Bookings/Alerts are labels; routes stay `credits`/`reservations`/`notifications`. **Wallet** = parked Credits emulator + LIVE cards sheet + parked Gift. **Visits** reads `useConsumerTickets`. **Orders** parked: no table/EF/type. **Reservations** = ONE feed, upcoming soonest-first then past newest-first. **Notifications = YOUR activity** (global parked with Social). **Detail stays at `/reservation/[id]`**.
- **Pay lists the closest 50 listed places** (device location, Monterrey fallback) — no tabs, no ticket rows, **no Open chip**, **no steps rail**. Header = `search/SearchBar` + ONE slot: live-ticket row when `tickets.active`, else the Activity link. Body leads with a **Cards** row on the shared `CardsModal` (Me keeps its door — `/me?cards=` is Stripe's return). 2+ chars call `consumer-web-suggest-places`. One tap creates the ticket at `base`, opening **THE TICKET** at `/visit/[id]` — **SEVEN steps (v4): Bill · Reward · Task · QR · Pay · Validate · Results** (`lib/ticket-journey.ts`). Hero panels use `TicketHero` (`rounded-panel`); never a raw 18/24/28px corner. Bill+tip guest-entered, **required on the QR**; tip rides the pre-discount subtotal, uncapped. Staff approve (freezes it) or send back ONE named fix. Live sync = `consumer-web-get-ticket` at 10s. Rates from `consumer-web-get-discount-quote`.
- **Proofs are screenshots** → `ticket-proofs` bucket (RLS: own folder), URL persisted. The declaration verifies; nothing reads the image yet (Ojo, staged).
- **Classes v2 — two axes, never merged.** `bronze ‹ silver ‹ gold ‹ diamond` earned/public; `free`/`premium` paid/private; LEGACY keys bridge in `class-context`. Me is flat; Class + Settings are modals. Favorites = localStorage. Referral = `/share`.
- Edge Functions: `consumer-web-*`. CI: `web-consumer.yml` (lint · typecheck · test · build).
