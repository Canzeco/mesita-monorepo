<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-consumer — consumer app (consumer.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first — the quickstart; Notion holds the deep docs. Package-specific rules only below.

- **Light theme + semantic tokens only** (`bg-card`/`text-foreground`/`border-border`), never `bg-zinc-900`/`text-white` — the ONE carve-out is `BalanceCard`'s photo face, bounded to that file. Surfaces read **premium**; wireframe stacks = regression. Map: Notion Docs › Design.
- **Overlay & loading primitives are mandatory:** route modals = `SlideOverShell`/`BottomSheetShell` from the segment `layout.tsx`, never `page.tsx`; state overlays = `LocalSheet`/`LocalDialog`, never `fixed inset-0`; loading = `Spinner`/`Skeleton`. **ONE semi-modal: `SEMI_MODAL_PANEL_CLASS` (80%) on BOTH sheet shells** — never hardcode a panel height. **A confirm is a `LocalDialog`, not a short sheet.** Z: BottomNav 40 · @modal 120 · local 130 · Toaster 140 · RouteBadge 150. Lint baseline **0**.
- **Parked blocks are not dead code** — check for a parking comment or live `@modal` first. Un-park = `soon: false` + the page body.
- **Four bottom tabs — Discover · Pay · Activity · Me**, plain labels; `/place` lights Discover. Routes: `consumer-route-contract.ts`. **Mobile mirror = SEPARATE PR.**
- **Discover = `/discover`, segments match labels** (the exception): feed · search · swipe · chat · favs. `HomeDeckBoundary` fetches the deck ONCE; switching never refetches. **Search IS the map** + the ONLY bar; results replace the rail, **never a lid**. **Feed** = `CatalogRails`, no bar. Default `/discover/search`; legacy paths 308 there in ONE hop, never chained (T4 caps 2).
- **ONE section-nav look:** `grid-flow-col auto-cols-fr` on `w-max min-w-full` — equal columns, scroller as FALLBACK. Discover 20%, Activity 25%, Pay 50%. Budgeted by the LONGEST label (+26px chrome): Discover's track is 5×widest+16 ≤ 359; **Activity's scrolls — accepted**. Re-measure before adding a pill.
- **`(shell)/layout.tsx`'s `getUser()` IS the auth wall**, segment-wide; `PROTECTED_PREFIXES` = edge fast path. `route-structure.test.tsx` pins: every page under `(shell)`; every `@modal` intercept in `isModalContractPath` (**miss it → BLANK modal**) + hard twin; no dangling redirect; one tab lit.
- **Third tab is LABELLED Pay** (QR icon); `/new-visit` creates a visit, `/visit/{id}` IS one. **A rename stops at the label** — never into routes, code or columns: object = ticket, column = `kind`. `/rewards*`, `/pay*`, `/qr`, `/ticket/:id` 308 forward. **`/visit/{id}` lights ACTIVITY**; `BottomNav` matches `matchPrefixes: string[]`. `checkout` means Stripe.
- **Activity = one container, FOUR sections, fixed order: Alerts · Visits · Orders · Reservations** (bare `/inbox` → visits: **default ≠ first, deliberately**; **order load-bearing**, pinned). Only **Alerts** differs from its route (`notifications`). **Wallet LEFT for Pay** (Activity holds events, a wallet holds instruments); `/inbox/credits` 308s to `/new-visit/wallet`. **Visits** reads `useConsumerTickets`. **Orders** parked: no table/EF/type. **Reservations** = ONE feed, upcoming soonest-first then past newest-first; detail at `/reservation/[id]`.
- **Pay = container, TWO sections: New · Wallet** (bare `/new-visit` is New and the default; Wallet = parked Credits emulator + LIVE cards sheet + parked Gift). Cards are `place.photos[0]` under a fixed scrim, ink face when absent; hold+bonus resolve through `controls_config` (default **3h**). Total is a LINE, never a hero — it cannot be spent. **New lists the closest 50 places** (device location, Monterrey fallback) — no ticket rows, **no Open chip**, **no steps rail**, **no per-row Soon badge**: payable sorts first with a Pay-here chip, the rest toast on tap. Header = `SearchBar` + ONE slot: the live-ticket row when `tickets.active`, else nothing. Me keeps its cards door (`/me?cards=` is Stripe's return). 2+ chars call `consumer-web-suggest-places`. One tap creates the ticket at `base`, opening **THE TICKET** at `/visit/[id]` — **SEVEN steps (v4): Bill · Reward · Task · QR · Pay · Validate · Results** (`ticket-journey.ts`). Hero panels use `TicketHero` (`rounded-panel`). Bill+tip guest-entered, **required on the QR**; tip rides the pre-discount subtotal, uncapped. Staff approve (freezes it) or send back ONE named fix. Sync = `consumer-web-get-ticket` 10s; rates from `consumer-web-get-discount-quote`.
- **Proofs are screenshots** → `ticket-proofs` bucket (RLS: own folder), URL persisted. Nothing reads it yet (Ojo, staged).
- **Classes v2 — two axes, never merged.** `bronze ‹ silver ‹ gold ‹ diamond` earned/public; `free`/`premium` paid/private; LEGACY keys bridge in `class-context`. Me is flat; Class + Settings modals. Favs = localStorage.
- Edge Functions: `consumer-web-*`. CI: `web-consumer.yml` (lint · typecheck · test · build).
