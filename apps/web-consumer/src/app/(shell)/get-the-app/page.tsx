import { DownloadAppScreen } from "@/components/consumer/DownloadAppScreen";

export const metadata = {
  title: "Get the app",
};

export default function GetTheAppPage() {
  return <DownloadAppScreen />;
}
