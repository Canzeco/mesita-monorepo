import { HelpModal } from "@/components/consumer/me/HelpModal";

// /me/help — how rewards work. Was a LocalSheet (MESITA-1789).
export const dynamic = "force-dynamic";

export default function Page() {
  return <HelpModal />;
}
