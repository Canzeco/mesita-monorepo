import { ClassModal } from "@/components/consumer/me/ClassModal";

// /me/class — the class ladder. Canonical again after MESITA-188's 308 onto the hub.
export const dynamic = "force-dynamic";

export default function Page() {
  return <ClassModal />;
}
