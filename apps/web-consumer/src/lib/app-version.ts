// ONE SOURCE FOR THE VERSION (MESITA-1641). It used to be a hardcoded string
// in a `<p>` at the bottom of Me. It is now on the About cell's summary line
// AND inside the About sheet, and two hand-typed copies of a number that
// changes every release is a drift bug waiting for its first release.
export const APP_VERSION = "v2.4.1";
