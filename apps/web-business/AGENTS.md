<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only.

- **Vercel** `mesita-web-business`, Root `apps/web-business`; `-legacy` is a decoy. Light theme, semantic tokens. CI `web-business.yml`; clients call `business-web-*` EFs.
- **Routes** (`lib/console-routes.ts`): **the address names its subject, and the PLACE is the only subject** — MESITA-1892 deleted the org tree, cookie and switcher. `/places/<id>/{profile,menus,reviews,visits,orders,reservations,pay,credits,admin,settings,products,products/{pay,terminal},customers,activity}` · `/places` the CATALOGUE (matrix, `?owned=`, Claim/Release) · `/places/new` · `/account`. **Bare `/places/<id>` 307s** to `/profile` and alone catches Stripe's stored `?connect=` → `products/pay`. Most have a **flat** twin, ONE file `(shell)/[flat]`: 307 onto the canonical (nothing selected → `NoPlaceYet`, unknown → 404). The rail links CANONICAL; `/` is 307, never 308. **No `next.config.ts` rule shadows a live address.**
- **Nav:** LEFT RAIL, **no wordmark**, **ONE COLUMN** from `RAIL_ROWS` — the only row list: Settings · Activity · Products, then the **eight products** in Pato's three groups, seam, Account. Groups are unnamed seams (`RAIL_GROUP_STARTS`). **ONE row shape**, no indent, one glyph, one pill, no id. `productRowHref` gives every product a place address. Menus/Reviews/Places/Admin keep addresses, lose rows; hidden ≠ protected (`tabsForAccess` gates). `RailScope.mode` `unknown|zero|solo|multi`: unknown = read failed (retry, never "create one"); zero = Add place alone; multi **never picks a place**. Selectors only at 2+. At `w-16`, chips only. Rows never dim; **Soon lives on the PAGE**. `Sidebar.tsx` is DARK, `--sidebar-*` only; `AppShell.tsx` resolves scope into `RailScopeContext`; only `main` scrolls.
- **One box, said once.** `shared/Section.tsx` IS the box: heading, fact body, action `right`. **Settings** (flat `/settings` is LIVE, never a redirect source): Team (ONE owner/editor/viewer surface), Developers. **Account is the person alone.** Never two badges for one fact.
- **Products is the catalogue**: `PartnerBanner` + eight cards, NOTHING else; Mesita Pay's Stripe account is `products/pay`, the payments log Activity. **A card states a fact the server READ** (`lib/products.ts`, pure): Profile is free; partner-gated read Locked, no verb; per-place print a count, a FAILED read none; Customers and Terminal are Soon. **Rewards is NOT a card** — it is Visits' second sentence; Visits keeps the container's state, never the dial's (or checkout at 0% reads Not enabled). A verb lands on the product's OWN view (`PRODUCT_VIEW` deleted; `PLACE_TABS` ⊇ `PRODUCT_KEYS`, pinned both ways). **`ZONE_ROWS` is keyed by product**: one `zone` per view via `ProductLadderTab`, no row in two.
- **Add place is a SEARCH** (Autocomplete + Text Search); each row states its state and verb. **A person may hold several places**; nothing caps the count.
- **Place heading, not a bar** (`PlaceHeading.tsx` is the `h1`); the rail learns pool places and unsaved edits from `OpenPlace.tsx` / `PlaceNavBridge.tsx`.
- **A place VIEW PAGE reads NOTHING.** The layout resolves the matrix once into `PlaceScope`; `PlaceTabGate` 404s.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER`; `SHELL_BLEED` is full-bleed. Pages are fragments with `loading.tsx`; forms cap at `FORM_COLUMN_CLASS`; save = `PlaceSaveBar`.
- **Never rebuild ticket UI**: Validate is staff-only.
- `plan=pro` is Mesita Partner, bought PER PLACE as the yearly **Mesita Membership** — the SKU, never the status. Roles owner/editor/viewer.
