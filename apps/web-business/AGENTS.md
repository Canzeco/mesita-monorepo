<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only.

- **Deploys** via Vercel `mesita-web-business`, Root Directory `apps/web-business` (`mesita-web-business-legacy` is a decoy). Light theme, semantic tokens.
- **Routes** (`lib/console-routes.ts`): **the address names its subject; ORG_PAGES = ORG_TARGETS = ORG_RAIL_TARGETS is ONE array.** `/places/<id>/{profile,menus,reviews,capabilities,rewards,admin}` · `/orgs/<id>/{settings,places(/new),customers,payments,activity}` · `/account` · `/orgs/new`. **Bare `/orgs/<id>` forwards** (307 → `/settings`) and alone reads Stripe's `?connect=`; mint Account Links with `orgRootHref`. Most names have a **flat** twin, ONE file `(shell)/[flat]`: 307 onto the canonical address for the remembered scope; nothing selected renders `NoPlaceYet`; unknown names 404. **`settings` and `places` have none**, and **no flat name may collide with a static directory**. The rail links CANONICAL. `/orgs/<id>/switch?to=` writes the org cookie, clears the place cookie. `/` is 307, never 308. **No `next.config.ts` rule may shadow a live address.**
- **Nav:** LEFT RAIL, **no wordmark**, **THREE SECTIONS**: Account (a link, no subitems) · **Organization selector** over Settings · Places · Customers · Payments · Activity · **Place selector** over Profile · Menus · Reviews · Capabilities · Rewards · Admin (super-admin only); Collapse alone in the footer. **Two shapes only** — ROW and SELECTOR; every page **indents under its selector**, one depth; no eyebrow, chip, box, tree line or `role="group"`; one seam, one pill, no id. **Ceremonies live in the selector menus** (Create organization · Add place · All places), never as rows; selectors guard first. At `w-16`: chips only, no indent. Rows never dim; Soon lives on the PAGE. `Sidebar.tsx` is DARK — `--sidebar-*` only, page tokens in `RailSelector.tsx`. `AppShell.tsx` resolves scope once into `RailScopeContext`; `main` is the only scroller; one breakpoint `lg`.
- **One box, said once.** `shared/Section.tsx` IS the box: name in the heading, fact in the body, action in `right` — never a description restating the title, nor an inner label repeating it. **No door rows**: Settings SHOWS its members and places. **Account is the person alone.** Never two badges for one fact.
- **Capabilities and Rewards are ONE engine, two displays.** `PromosSection` takes a `zone`; `ZONE_ROWS` (`sections/controls/offerings.ts`) owns the split, proven total by test. Never fork it.
- **Never rebuild ticket UI**: Validate is the staff surface; `business-web-*-ticket*` off limits.
- **Place heading, not a bar.** `PlaceHeading.tsx` is the `h1`. The rail learns a pool place from `OpenPlace.tsx`, unsaved edits from `PlaceNavBridge.tsx` — the only reader of `PlaceContext`.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER`, the rail outside it; full-bleed uses `SHELL_BLEED`; `PlaceStatesTable` never sets `w-full`. Pages are fragments with `loading.tsx`; forms cap at `FORM_COLUMN_CLASS`; save = `PlaceSaveBar`.
- Plan SKUs: **Free** + **Verified** (`plan=pro`); Partnership free. **"Membership" banned.** Roles: owner/editor/viewer.
- CI: `web-business.yml` — lint · typecheck · test · build, path-filtered. Clients call `business-web-*` Edge Functions.
