<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only below.

- **Deploys** via Vercel `mesita-web-business`, Root Directory `apps/web-business`; `mesita-web-business-legacy` is a decoy. Light theme + semantic tokens. Calm, high-density — don't ornament.
- **Routes** (`lib/console-routes.ts`): **the address names its subject.** Canonical `/places/<id>/{profile,reviews,capabilities,rewards,admin}` · `/orgs/<id>/{organization,payments,credits,activity,members,places(/new)}` · `/account` (you, the switchers) · `/orgs/new`. Each has a **flat** twin (`/profile`, `/credits`, …) live as a **resolver** (`lib/flat-address.tsx`): 307 onto the canonical address for the remembered place/org (`lib/selected-place.ts`, the two rail cookies); nothing selected renders `NoPlaceYet`. The rail links CANONICAL — one hop, shareable; flat is for bookmarks and Stripe returns. `/orgs/<id>` forwards (writes the org cookie, clears the place cookie, lands on `payments`, honours `?to=<flat>`); Stripe links ride it. `/` resolves, 307 — never 308. `next.config.ts` forwards retired spellings; **no rule there may shadow a live address** — redirects run before filesystem routes and a swallowed page stays green — so `legacy-redirects.test.ts` walks every address, canonical AND flat, through that table.
- **Nav:** LEFT RAIL, **two levels**: the organization's four (Organization · Payments · Credits · Activity), the place's name as a group header, its five indented (Profile · Reviews · Capabilities · Rewards · Admin, super-admin only), then Account below a seam. No switcher, Plus, chip or box; ONE indent (`RAIL_INDENT`, never at `w-16`), one seam, exactly one pill, no visible id. Members and Places have no row and light Organization, whose page is their door. With no place the place rows stay at FULL strength — live links, never dimmed — and the page answers with Add place. `AppShell.tsx` (`fixed inset-0` + `overflow-clip`) resolves scope once and provides `RailScopeContext` (Account's `ScopeSwitchers` reads it); `main` is the only scroller; one breakpoint `lg`. `Sidebar.tsx` is DARK: `--sidebar-*` dock tokens (`globals.css`), never a page token; `w-60`/`w-16`. Every page has `loading.tsx`. `ConsoleHeader.tsx`: crumb + route.
- **Capabilities and Rewards are ONE engine, two displays.** `PromosSection` takes a `zone`; `ZONE_ROWS` (`sections/controls/offerings.ts`) owns the split, proven total by test. Never fork the ladder — its rungs depend on each other.
- **Never rebuild ticket UI** — Mesita Validate is the staff surface; leave `business-web-*-ticket*` alone.
- **Place heading, not a bar.** `PlaceHeading.tsx` is the `h1`. Rail learns a pool place from `OpenPlace.tsx`; unsaved-edits from `PlaceNavBridge.tsx`, the only console component that may read `PlaceContext`.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER`; rail sits outside. Full-bleed uses `SHELL_BLEED`. `PlaceStatesTable` never sets `w-full`. Pages return a fragment. Forms cap at `FORM_COLUMN_CLASS`. Save = `PlaceSaveBar`.
- Plan SKUs: **Free** + **Verified** (`plan=pro`). Partnership is free. **"Membership" banned.** Roles: `owner`/`editor`/`viewer`.
- Clients never call the DB — `business-web-*` Edge Functions.
- CI: `web-business.yml` — lint · typecheck · test · build (Node 22+), path-filtered.
