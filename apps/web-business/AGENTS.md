<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only below.

- **Deploys** via Vercel `mesita-web-business`, Root Directory `apps/web-business`; `mesita-web-business-legacy` is a decoy. Light theme, semantic tokens, high-density.
- **Routes** (`lib/console-routes.ts`): **the address names its subject; every org page is a named segment.** Canonical `/places/<id>/{profile,reviews,capabilities,rewards,admin}` · `/orgs/<id>/{organization,customers,payments,activity,members,places(/new)}` · `/account` · `/orgs/new`. **Bare `/orgs/<id>` forwards** (307 → `/organization`) and is the only route reading Stripe's stored `?connect=`; mint Account Links with `orgRootHref`. Each has a **flat** twin (`/profile`, `/customers`, …), all ten served by ONE file `(shell)/[flat]` over `lib/flat-address.tsx`: 307 onto the canonical address for the remembered place/org (`lib/selected-place.ts`, two cookies); nothing selected renders `NoPlaceYet`; unknown names 404. **No flat name may collide with a static directory** (static wins; a test asserts both ways). The rail links CANONICAL. `/orgs/<id>/switch?to=<flat>` is the switcher's forwarder (writes the org cookie, clears the place cookie). `/` resolves 307, never 308. **No `next.config.ts` rule may shadow a live address**; `legacy-redirects.test.ts` walks every one through that table.
- **Nav:** LEFT RAIL, **no wordmark**, **ONE flat column**: Account · Organization · Customers · Payments · Activity · Places, then the place's five **indented under Places** (Admin super-admin only). Collapse alone in the footer. **One row shape everywhere**, **one indent, one depth**: no group header, eyebrow, switcher, Plus, chip, box, tree line or `role="group"`; one seam, one pill, no name, no id, **no Soon badge** — an unbuilt row is full-strength and its PAGE says Soon. Members is the one org page with no row and lights Organization, its door. With no place the place rows stay at full strength, never dimmed. Mobile topbar: the scope (`org · place`), no logo. `AppShell.tsx` (`fixed inset-0`, `overflow-clip`) resolves scope once into `RailScopeContext`; `main` is the only scroller; one breakpoint `lg`. `Sidebar.tsx` is DARK: `--sidebar-*` dock tokens, never a page token; `w-60`/`w-16`. Every page has `loading.tsx`.
- **Capabilities and Rewards are ONE engine, two displays.** `PromosSection` takes a `zone`; `ZONE_ROWS` (`sections/controls/offerings.ts`) owns the split, proven total by test. Never fork the ladder — its rungs interlock.
- **Never rebuild ticket UI**: Validate is the staff surface; leave `business-web-*-ticket*` alone.
- **Place heading, not a bar.** `PlaceHeading.tsx` is the `h1`. The rail learns a pool place from `OpenPlace.tsx`, unsaved edits from `PlaceNavBridge.tsx` — the only component that may read `PlaceContext`.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER`, the rail outside it; full-bleed uses `SHELL_BLEED`. `PlaceStatesTable` never sets `w-full`. Pages return a fragment. Forms cap at `FORM_COLUMN_CLASS`. Save = `PlaceSaveBar`.
- Plan SKUs: **Free** + **Verified** (`plan=pro`); Partnership is free. **"Membership" banned.** Roles: `owner`/`editor`/`viewer`.
- Clients never call the DB — `business-web-*` Edge Functions.
- CI: `web-business.yml` — lint · typecheck · test · build (Node 22+), path-filtered.
