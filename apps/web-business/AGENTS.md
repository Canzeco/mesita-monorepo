<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only.

- **Vercel** `mesita-web-business`, Root `apps/web-business`; `-legacy` is a decoy. Light theme, semantic tokens. CI `web-business.yml`; clients call `business-web-*` EFs.
- **Routes** (`lib/console-routes.ts`): **the address names its subject, and the PLACE is the only subject**. `/places/<id>/{profile,visits,orders,reservations,rewards,pay,pay/setup,credits,admin,setup,activity}` · `/places` the CATALOGUE (matrix, `?owned=`, Claim/Release) · `/places/new` · `/settings`, NOT place-scoped and NOT a flat twin. **Bare `/places/<id>` 307s** to `/profile` and alone catches Stripe's stored `?connect=` → `pay/setup`. Most have a **flat** twin, ONE file `(shell)/[flat]`: 307 onto the canonical (nothing selected → `NoPlaceYet`, unknown → 404). The rail links CANONICAL; `/` is 307. **No `next.config.ts` rule shadows a live address.**
- **Nav: FOUR DESTINATIONS** — Place · Setup · Activity · Settings. Place is the venue band, Settings the pinned foot, `RAIL_ROWS` the two between. No sections, heads or seam. **ONE row shape**, one glyph, one pill, no id. Settings stays place-free: Sign out is there and rows draw only in `solo`/`multi`. Product views keep addresses, lose rows; **Menus/Reviews are CARDS ON PROFILE**; hidden ≠ protected (`tabsForAccess` + `PlaceTabGate`). `RailScope.mode` `unknown|zero|solo|multi`: unknown = read failed (retry, never "create one"); zero = Add place alone; multi **never picks a place**. NO selector; **All places** is a `multi`-only row. **ONE WIDTH**, no collapse. Rows never dim; **Soon lives on the PAGE**. `Sidebar.tsx` is DARK, `--sidebar-*` only; `AppShell.tsx` resolves scope into `RailScopeContext`.
- **One box, said once.** `shared/Section.tsx` IS the box: heading, fact body, action `right`. **Settings** is You, Team (ONE owner/editor/viewer surface) and Developers on one screen; `/account` forwards to it. Never two badges for one fact.
- **Setup is the shop AND the switchboard** (was Products): `PartnerBanner` + the cards, nothing else; Payments' Stripe account is `pay/setup`, under its own product; the payments log is Activity. **A card states a fact the server READ** (`lib/products.ts`, pure): Profile is free; partner-gated read Locked, no verb; per-place print a count, a FAILED read none; Customers is Soon. **Rewards is a card and a view**: its state is the dial, Visits' the container's; Visits' zone is EMPTY, never `guestSummary`d. A verb lands on the product's OWN view (`PLACE_TABS` ⊇ `PRODUCT_KEYS`, pinned both ways). **`ZONE_ROWS` is keyed by product**: one `zone` per view via `ProductLadderTab`, no row in two.
- **Add place is a SEARCH** (Autocomplete + Text Search); each row states its state and verb. **A person may hold several places**; nothing caps the count.
- **Place heading, not a bar** (`PlaceHeading.tsx` is the `h1`); the rail learns pool places and unsaved edits from `OpenPlace.tsx` / `PlaceNavBridge.tsx`.
- **A place VIEW PAGE reads NOTHING.** The layout resolves the matrix once into `PlaceScope`; `PlaceTabGate` 404s.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER`; `SHELL_BLEED` is full-bleed. Pages are fragments with `loading.tsx`; forms cap at `FORM_COLUMN_CLASS`; save = `PlaceSaveBar`.
- **Never rebuild ticket UI**: Validate is staff-only.
- `plan=pro` is Mesita Partner, bought PER PLACE as the yearly **Mesita Membership** — the SKU, never the status. Roles owner/editor/viewer.
