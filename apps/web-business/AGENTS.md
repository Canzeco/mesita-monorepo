<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only.

- **Deploys** via Vercel `mesita-web-business`, Root Directory `apps/web-business` (`mesita-web-business-legacy` is a decoy). Light theme, semantic tokens, dense.
- **Routes** (`lib/console-routes.ts`): **the address names its subject; every org page is a named segment.** `/places/<id>/{profile,reviews,capabilities,rewards,admin}` · `/orgs/<id>/{organization,customers,payments,activity,places(/new)}` · `/account` · `/orgs/new`. **Bare `/orgs/<id>` forwards** (307 → `/organization`) and alone reads Stripe's stored `?connect=`; mint Account Links with `orgRootHref`. Every canonical name has a **flat** twin, all served by ONE file `(shell)/[flat]`: 307 onto the canonical address for the remembered place/org (`lib/selected-place.ts`); nothing selected renders `NoPlaceYet`; unknown names 404; **no flat name may collide with a static directory**. The rail links CANONICAL. `/orgs/<id>/switch?to=` writes the org cookie, clears the place cookie. `/` is 307, never 308. **No `next.config.ts` rule may shadow a live address** (a test walks every one).
- **Nav:** LEFT RAIL, **no wordmark**, **ONE flat column**: Account · Organization · Customers · Payments · Activity · Places, then the place's five **indented under Places** (Admin super-admin only); Collapse alone in the footer. **One row shape; one indent, one depth**: no header, eyebrow, switcher, chip or tree line; one seam, one pill, no name, no id, **no Soon badge** — an unbuilt row is full-strength; its PAGE says Soon. Every org address is a row; place rows never dim. Mobile topbar: the scope, no logo. `AppShell.tsx` (`fixed inset-0`, `overflow-clip`) resolves scope once into `RailScopeContext`; `main` is the only scroller; one breakpoint, `lg`. `Sidebar.tsx` is DARK — `--sidebar-*` dock tokens only; `w-60`/`w-16`. Every page has `loading.tsx`.
- **One box, said once.** `shared/Section.tsx` IS the box: name in the heading, fact in the body, action in `right` — never a description restating the title, an eyebrow above it, or an inner label repeating it. **No door rows**: Organization SHOWS its members and places. **Account is the person alone**; the org selector (`OrgSwitcher`) lives on Organization. Never two badges for one fact.
- **Capabilities and Rewards are ONE engine, two displays.** `PromosSection` takes a `zone`; `ZONE_ROWS` (`sections/controls/offerings.ts`) owns the split, proven total by test. Never fork it.
- **Never rebuild ticket UI**: Validate is the staff surface; `business-web-*-ticket*` off limits.
- **Place heading, not a bar.** `PlaceHeading.tsx` is the `h1`. The rail learns a pool place from `OpenPlace.tsx`, unsaved edits from `PlaceNavBridge.tsx` — the only reader of `PlaceContext`.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER`, the rail outside it; full-bleed uses `SHELL_BLEED`; `PlaceStatesTable` never sets `w-full`. Pages return a fragment; forms cap at `FORM_COLUMN_CLASS`; save = `PlaceSaveBar`.
- Plan SKUs: **Free** + **Verified** (`plan=pro`); Partnership is free. **"Membership" banned.** Roles: owner/editor/viewer.
- CI: `web-business.yml` — lint · typecheck · test · build (Node 22+), path-filtered. Clients call `business-web-*` Edge Functions.
