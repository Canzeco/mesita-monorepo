<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first. Package rules only.

- **Vercel** `mesita-web-business`, Root `apps/web-business`; `-legacy` is a decoy. Light theme, semantic tokens. CI `web-business.yml`: lint · typecheck · test · build; clients call `business-web-*` EFs.
- **Routes** (`lib/console-routes.ts`): **the address names its subject; ORG_PAGES = ORG_TARGETS, ONE array.** `/places/<id>/{profile,menus,reviews,capabilities,rewards,admin}` · `/orgs/<id>/{settings,products(/pay),places(/new),customers,activity}` · `/account` · `/orgs/new`. **Bare `/orgs/<id>` 307s** to `/settings` and alone reads Stripe's `?connect=` → `products/pay`; its `orgRootHref` mint is walked by `legacy-redirects.test.ts`. Most have a **flat** twin, ONE file `(shell)/[flat]`: 307 onto the canonical; nothing selected renders `NoPlaceYet`; unknown 404. The rail links CANONICAL. `/orgs/<id>/switch?to=` writes the org cookie, clears the place; `/` is 307, never 308. **No `next.config.ts` rule shadows a live address.**
- **Nav:** LEFT RAIL, **no wordmark**, **ONE FLAT COLUMN** from `RAIL_ROWS` — the only row list: Settings · Products · Profile · Menus · Reviews · Customers · Activity, seam, Account. **ONE row shape**, no indent, one glyph, one pill, no id. Capabilities/Rewards/Places/Admin keep addresses, lose rows; hidden ≠ protected (`tabsForAccess` gates). `RailScope.mode` is `unknown|zero|solo|multi`: unknown = read failed (retry, never "create one"); zero = org rows plus the Add ceremony; multi shows the place selector and **never picks a place**. Selectors render only at 2+ orgs / places. At `w-16`, chips only. Rows never dim; **Soon lives on the PAGE**. `Sidebar.tsx` is DARK, `--sidebar-*` only; `AppShell.tsx` resolves scope into `RailScopeContext`; only `main` scrolls.
- **One box, said once.** `shared/Section.tsx` IS the box: name heading, fact body, action `right`. **Settings** (gear; flat `/settings` is LIVE, never a redirect source): Members, Developers. **Account is the person alone.** Never two badges for one fact.
- **Products is the catalogue**: `PartnerBanner` + eight cards, NOTHING else; Mesita Pay's Stripe account is `products/pay`, a sub-step. Payments log: Activity. **A card states a fact the server READ** (`lib/products.ts`, pure): Profile is free; partner-gated read Locked, no verb; per-place print a count, a FAILED read none. Verbs land via `PRODUCT_VIEW` on the view holding the switch — hand-written; `ZONE_ROWS` cannot derive it. Terminal is the one Soon.
- **Add place is a SEARCH** (Autocomplete + Text Search); each row states its state and verb. **One place per org** is a console rule: the door hides and `requireCeremonyOwner` refuses; never a DB constraint.
- **Capabilities and Rewards are ONE engine.** `PromosSection` takes a `zone`; `ZONE_ROWS` (`sections/controls/offerings.ts`) owns the split.
- **Place heading, not a bar.** `PlaceHeading.tsx` is the `h1`; the rail learns pool places from `OpenPlace.tsx`, unsaved edits from `PlaceNavBridge.tsx`.
- **A place VIEW PAGE reads NOTHING.** The layout resolves the matrix once into `PlaceScope`; `PlaceTabGate` 404s. `cache()` dedupes per REQUEST, so a fetch is paid every click.
- **Layout:** fluid, no max-width. `(shell)/layout.tsx` sets `SHELL_GUTTER` (and `viewerError`, which feeds `railMode`); `SHELL_BLEED` is full-bleed. Pages are fragments with `loading.tsx`; forms cap at `FORM_COLUMN_CLASS`; save = `PlaceSaveBar`.
- **Never rebuild ticket UI**: Validate is staff-only.
- `plan=pro` is Mesita Partner, bought as the yearly org **Mesita Membership** — the SKU, never the status. Roles owner/editor/viewer.
