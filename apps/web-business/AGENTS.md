<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-business — business console (business.mesita.ai)

> Read root [`CLAUDE.md`](../../CLAUDE.md) first — the quickstart; Notion holds the deep docs. Package-specific rules only below.

- **Deploys via the Vercel `mesita-web-business` project** on `Canzeco/mesita-monorepo`, Root Directory `apps/web-business`. `mesita-web-business-legacy` is a decoy; a wrong connection = silent no-deploy.
- Light theme + semantic tokens. Business surfaces stay **calm and high-density** — don't ornament them.
- **Four screens:** Account · Organization · Places · Place (`SHELL_ROUTES` in `lib/console-routes.ts`; a test asserts each maps to a route file). **Org Places / Public Places are rail LABELS, not screens** — saved `?owned=` filters on the one merged list, pinned by `console-routes.test.ts`. `/places/<id>` is the ONE place console, four tabs — **Profile · Capabilities · Activity · Admin** (`PlaceTabs.tsx`); Admin renders for super-admins only and every box on it calls `admin-web-*`. `next.config.ts` redirects every legacy URL here; a contract test proves none 404s. **Never rebuild console ticket UI**: Mesita Validate (`apps/web-validate`) is the whole staff ticket surface — leave the `business-web-*-ticket*` EFs alone.
- **Nav is a LEFT RAIL.** `AppShell.tsx` pins the frame (`fixed inset-0` + `overflow-clip`) and `main` is the ONLY scroller. `Sidebar.tsx` is the rail: light `--sidebar` tokens, never admin's inversion; `w-60` ↔ `w-16`, cookie-persisted; drawer below `lg`; every text token a semantic pair, never an opacity fraction. `ConsoleHeader.tsx` carries the breadcrumb + route readout the chromeless window needs. **One breakpoint, `lg`.** `PLACEBAR_STICKY_CLASS` is a flat `sticky top-0` — the header is main's SIBLING, so there is no offset to encode. The rail learns the open place's name via `OpenPlace.tsx` (the child publishes up); **navigation never travels up** — the four tabs stay in `PlaceBar`, the only component that can reach `PlaceProvider`.
- **Layout:** the shell owns the container and it is FLUID, no max-width — `(shell)/layout.tsx` sets the gutter once (`SHELL_GUTTER`) and the rail sits outside it. A full-bleed child cancels the gutter with `SHELL_BLEED`; the two are exact negatives and `shell-chrome.test.ts` fails if they drift. Pages return a bare fragment and inherit `flex flex-col gap-4`; they never set their own width. Section cards are `Section.tsx`; forms cap at `FORM_COLUMN_CLASS` (`lib/ui-classes.ts`) — cap the form, not the card. The `place-manage` sections are a masonry (`columns-1 lg:columns-2 xl:columns-3`), not a grid. Global save = the floating `PlaceSaveBar`.
- Nomenclature: businesses subscribe to a **plan**. Sold SKUs are **Free** + **Verified** (`plan=pro`); joining Partnership is free. **"Membership" is banned in copy** — what a place joins is **Partnership**. The enum still carries `ultra`; the console folds it onto Verified. Member roles: `owner`/`editor`/`viewer`.
- Clients never call the DB — everything via `business-web-*` Edge Functions.
- CI: `web-business.yml` — lint · typecheck · build, all blocking (Node 22+), path-filtered to `apps/web-business/**`.
