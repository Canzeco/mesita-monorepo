import { VisitsClient } from "../VisitsClient";

// Visits — the local ladder. Config state lives in the layout (PromosState),
// so this page is purely the view.
export default function PromosVisitsPage() {
  return <VisitsClient />;
}
