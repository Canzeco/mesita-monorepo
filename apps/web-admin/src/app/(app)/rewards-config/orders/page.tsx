import { OrdersClient } from "../OrdersClient";

// Orders — the remote ladder, parked. No Class box: class prices presence and
// a delivery order has nobody in the chair.
export default function PromosOrdersPage() {
  return <OrdersClient />;
}
