import { LanesPanel } from "./LanesPanel";

// Scores & Lanes — how the subscores combine into a score per lane, and how
// the three lanes merge into the final deck. Carries the Deck playground.
export default function ScoringLanesPage() {
  return <LanesPanel />;
}
