<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-admin — internal admin console

> Read root [`CLAUDE.md`](../../CLAUDE.md) first; Notion holds the deep docs. Package rules only below.

- Light theme + semantic tokens; calm, dense, unornamented. Design map: Notion Docs › Design. New UI takes chrome from [`src/components/admin-ui/`](./src/components/admin-ui/), never a route-local shim. `/brand` is read-only, out of the sidebar.
- Clients never touch the DB — everything via `admin-web-*` Edge Functions. The EF-invoke wrapper is the deliberate **`Result` variant** (consumer/business throw `EFError`) — keep it.
- Every config page: server seeds → client form → **WHOLE-BLOB save** to one `app_config` column, normalizer in lockstep, `dirty` gated on `loadError`. A knob nothing reads is **STAGED**; a page with none renders **`ConfigSoon`** and says **Soon**; unenforced config is a bug.
- **A label never repeats its section heading** — Configurations › **Enrichment**, never "Enricher Config"; tabs too. Labels live in each route's `nav.ts` and **a rename stops there**: routes, blobs, EF names never follow (**Discovery** = `/filters-config`). **Sidebar in product-flow order** — `CONFIGS_NAV` (`src/app/(app)/configs-nav.ts`) is the list of record, never mirrored. No Atlas page (spec: Docs › Atlas). **Verification** = PLACE ownership proof, **Ojo** = reading a guest's screenshot. **Memo is not a row** — it IS the chat engine, so it belongs to Discovery; it has no editor — `memo_config` is EF-served.
- **Discovery** (`/filters-config`) is ONE flat page: a **Signals** table (**exponent** editable per row) and a **Bought slots** card. **TWO LANES that never mix** — six EARNED signals (proximity · timing · category · popularity · semantic · randomness) compose as `s^w`; **Promoting is not one of them**, money buys a deck POSITION, never a score. Blob `app_config.discovery_config`, vocabulary CODE-DEFINED in `_shared/discovery-signals.ts`. **Enforced on Swipe**; other engines wired as rebuilt.
- **Visits Config** = the LOCAL context (THE TICKET); **Orders Config** = the REMOTE one — both **Soon** today, blobs and EFs untouched. **Neither prices anything** — rates live in the Promos grid. Premium clamps up to the Free quota; the preselected tip must be a chip that exists.
- **Promos Config** (`/rewards-config`, tabs Tiers · Distribution) edits COMPONENTS; storage keeps the GRID: `standing = base + classStep + planStep`. **Tiers is SIX boxes, one per (paid strategy × context)** — a place picks one strategy, so its column IS its whole program. **Orders has no Class rows**, **parked**. **Bonuses are per strategy AND per context**, fanned out on read. `additivityError` REFUSES a grid not expressible as components, or whose class offsets decrease, so the ladder cannot invert; only `google-vs-story` warns. **One Save, hoisted to the layout**; Distribution read-only. Twin: `web-business/lib/business/promos.ts`.
- `database.types.ts` is hand-copied across the web apps — regenerate from cloud, never hand-edit. CI `web-admin.yml`: lint · typecheck · build, Node 22+, path-filtered.
