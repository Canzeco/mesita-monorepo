<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-admin — internal admin console

> Read root [`CLAUDE.md`](../../CLAUDE.md) first; Notion holds the deep docs. Package rules only below.

- Light theme + semantic tokens. Map: Notion Docs › Design. New UI takes chrome from [`src/components/admin-ui/`](./src/components/admin-ui/), never a route-local shim. `/brand` is read-only, unlisted.
- Clients never touch the DB — all via `admin-web-*` EFs. EF-invoke is the **`Result` variant** (consumer/business throw `EFError`).
- Every config page: server seeds → client form → **WHOLE-BLOB save** to one `app_config` column, normalizer in lockstep, `dirty` gated on `loadError`. A knob nothing reads is **STAGED**.
- **A label never repeats its section heading** — Configurations › **Intake**. Labels live in `nav.ts`; routes, blobs, EF names never follow. Sidebar: `CONFIGURATIONS_NAV`. **Intake** = Intaker (Create **1–5**, Enrich **1–10**; 10 = Embedding). One box: Create · Enrich · Update · Create + Enrich. Update: Listed · Active · Verified · Partner · Visit Rewards; Active off unlists. Admin statuses: General · Partnership · Intake. Search eligibility: Discovery › Map; Multiple Places: one query/line. Single Place names use Deep; empty lists Mesita. **Verification** = place ownership; **Ojo** = guest screenshot. Memo powers Discovery chat; Chat writes `discovery_config.chat.prompt`.
- **Discovery** (`/filters-config`): two subpages, mode → modules → Lineup. **Modes** (`/modes`) — Fast is Autocomplete only. Deep merges Autocomplete + Text Search + Lineup Name (`places.name`, not `google_name`). Map + Swipe Lineup live; Home Soon. Google types are **not** here. **Modules** (`/modules`) — Google types (`general.categoryCount` + batteries) · Autocomplete · Nearby · Text Search · Places Lineup · Social Lineup + Perplexity Soon. Lineup = the eight signals in `_shared/discovery-signals.ts`; `semantic` → Summary. Slice Save. Filters persist, no knobs.
- **Visits** (`/visits-config`): Bill · Sync · Report (`visits_config`) plus **Ojo** (`ojo_config`, own Save). Unwired keys stay in that blob, off the HTML. **Orders Config** = the REMOTE context. Neither prices — rates are Promos. **Controls** (`/controls-config`, `controls_config`): the Wallet's Credits hold (**3h**) + bonus — FALLBACKS a place may override, capped. Gifting Soon. Premium clamps up to the Free quota; the preselected tip must be a chip that exists.
- **Promos Config** (`/rewards-config`, **one page**) edits VISIT COMPONENTS only. Three super boxes: **Strategies · Discount Cap · Expected Distribution**. One Save; `/tiers` `/distribution` redirect. Twin: `web-business/lib/business/promos.ts`. **Single Place › Controls** is TWO ZONES: **Offerings** (0–7 meter + a dependency ladder, each locked row naming its prerequisite) and **Settings** (staff PIN, Team) — **never a Tutorial box.** Quick view = pipeline (Active…Verified) then commercial (Offerings · Partner · Visit Rewards · Mesita Pay · Credits). **"promo"/"membership" BANNED in copy.**
- `database.types.ts` is hand-copied across web apps — regenerate from cloud, never hand-edit. CI `web-admin.yml`: lint · typecheck · build, Node 22+, path-filtered.
