<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-admin — internal admin console

> Monorepo-wide rules: root [`CLAUDE.md`](../../CLAUDE.md) (the quickstart) — read it first. This file adds only package-specific rules.

- Light theme + semantic tokens; calm and high-density — don't ornament. Design map: Notion Product Rules §F. New UI imports shared chrome from [`src/components/admin-ui/`](./src/components/admin-ui/); don't grow route-local shims. `/brand` is read-only and stays out of the sidebar.
- Clients never call the DB — everything via `admin-web-*` Edge Functions. The EF-invoke wrapper is the deliberate **`Result` variant** (consumer/business throw `EFError`); keep the divergence, dedupe only the plumbing.
- **A label never repeats its section heading**: the group says Configurations, so the item is **Atlas**, not "Atlas Config" — same rule for in-page tabs. Labels live in each route's `nav.ts`.
- **Configurations are ordered by product flow**: Admin · Models · Sourcing · Atlas · Enricher · Verification · Ojo · Filters · Promos · Reservations. **Atlas** (profile-spec) and **Enricher** (pipeline behavior) are separate parents — never merge them. **Verification** (`/verification-config`) governs PLACE ownership proof; **Ojo** (`/ojo-config`) governs the engine reading a guest's story/review screenshot, every knob **staged** until it ships. **Memo is not a row** — Home › Chat IS Memo, so it lives in Filters › Chat (`/memo-config` 308s there).
- **Filters Config** (`/filters-config`) REPORTS contradictions, never auto-corrects. **General** carries the law — which of the six modules exist (Context · Where · Distance · When · What · Random), defaults, bounds, behavior — and six surface tabs (**Swipe · Catalog · Chat · Social · Map · Search**) inherit or override it. **`inherit` TRACKS General**; General's master switch beats a surface saying `on`. Blob `app_settings.filters_config.v1`, WHOLE-BLOB write, normalizer in lockstep. Every knob is **`not-wired`**; Catalog · Chat · Social are also **surface parked**. **Chat alone carries a second strip**: Memo · Filters · Playground · Data Access, one Save per tab.
- **Promos Config** (`/rewards-config`, tabs **Visits · Orders · Distribution**) edits COMPONENTS; storage keeps the GRID: `standing = base + classStep + planStep`, steps per-strategy. Visits has five boxes — **Base is the floor; Welcome · Class · Plan · Actions are signed adders** (pinned rungs show `—`). Orders has no Class box (class prices presence) and is **parked**. `additivityError` REFUSES a grid not expressible as components, or whose class offsets decrease — that guard, not a warning, is why the ladder cannot invert; only `google-vs-story` remains in `modelWarnings`. **One Save, hoisted to the layout**: subpages are views on one blob. Class/plan axes and the legacy-key bridge: `supabase/CLAUDE.md`. Twin to keep in lockstep: `web-business/src/lib/business/promos.ts`.
- `database.types.ts` is hand-copied across the web apps — regenerate from cloud, never hand-edit.
- CI: `web-admin.yml` — lint · typecheck · build (Node 22+), path-filtered to `apps/web-admin/**`.
