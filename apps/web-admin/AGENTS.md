<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-admin — internal admin console

> Monorepo-wide rules: root [`CLAUDE.md`](../../CLAUDE.md) (the quickstart) — read it first. This file adds only package-specific rules.

- Light theme + semantic tokens; calm and high-density — don't ornament. Design map: Notion Docs › Design. New UI takes chrome from [`src/components/admin-ui/`](./src/components/admin-ui/), never a new route-local shim. `/brand` is read-only, out of the sidebar.
- Clients never touch the DB — everything via `admin-web-*` Edge Functions. The EF-invoke wrapper is the deliberate **`Result` variant** (consumer/business throw `EFError`); keep the divergence.
- Every config page: server seeds → client form → **WHOLE-BLOB save** to one `app_config` column, normalizer in lockstep with `_shared/*-config.ts`, `dirty` gated on `loadError`. A knob nothing reads is **STAGED** — unenforced config is a bug.
- **A label never repeats its section heading** — Configurations › **Enrichment**, never "Enricher Config"; same for tabs. Labels live in each route's `nav.ts` and **a rename stops there**: routes, blobs and EF names never follow (**Discover** = `/filters-config`). **Ordered by product flow**: Admin · Models · Sourcing · Enrichment · Verification · Discover · Reservations · Orders · Promos · Ojo. No Atlas page — spec is Notion 🗺️ Docs › Atlas. **Verification** governs PLACE ownership proof; **Ojo**, the engine reading a guest's screenshot. **Memo is not a row** — Home › Chat IS Memo, so it lives in Filters › Chat.
- **Filters Config** REPORTS contradictions, never auto-corrects. **General carries the law** — which modules exist, their defaults and bounds — and the surface tabs inherit or override it; **`inherit` TRACKS General**, whose master switch beats a surface saying `on`. Blob `filters_config.v1`; every knob **`not-wired`**; Catalog · Chat · Social **parked**. **Chat alone carries a second strip**, one Save per tab.
- **Orders Config** is the REMOTE context — channels, fulfilment, per-plan quotas, eligibility. **Rates are NOT here**: an order's pay is the Promos grid's parked `orders` column. Premium clamps up to the Free quota.
- **Promos Config** (`/rewards-config`, tabs **Tiers · Distribution**) edits COMPONENTS; storage keeps the GRID: `standing = base + classStep + planStep`. **Tiers is FOUR boxes, one per (strategy × context)** — a place picks one strategy, so its column IS its program. Boxes read floor-first, then signed adders. **Orders has no Class rows** and is **parked**. **Bonuses are per strategy AND per context**, fanned out on read — no version bump. `additivityError` REFUSES a grid not expressible as components, or whose class offsets decrease — why the ladder cannot invert, leaving only `google-vs-story` in `modelWarnings`. **One Save, hoisted to the layout**; Distribution read-only. Twin: `web-business/src/lib/business/promos.ts`.
- `database.types.ts` is hand-copied across the web apps — regenerate from cloud, never hand-edit. CI: `web-admin.yml` — lint · typecheck · build (Node 22+), path-filtered.
