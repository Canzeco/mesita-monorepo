<!-- GENERATED — scripts/sync-rules.ts mirrors this file from its sibling CLAUDE.md. Edit CLAUDE.md (root: below its END marker) or scripts/rules-quickstart.md — NEVER this file. -->
# apps/web-admin — internal admin console

> Read root [`CLAUDE.md`](../../CLAUDE.md) first; Notion holds the deep docs. Package rules only below.

- Light theme + semantic tokens. Map: Notion Docs › Design. New UI takes chrome from [`src/components/admin-ui/`](./src/components/admin-ui/), never a route-local shim. `/brand` is read-only, unlisted.
- Clients never touch the DB — all via `admin-web-*` EFs. EF-invoke is the **`Result` variant** (consumer/business throw `EFError`).
- Every config page: server seeds → client form → **WHOLE-BLOB save** to one `app_config` column, normalizer in lockstep, `dirty` gated on `loadError`. A knob nothing reads is **STAGED**.
- **A label never repeats its section heading** — Configurations › **Crenup**. Labels live in `nav.ts`; routes, blobs, EF names never follow. Sidebar: `CONFIGURATIONS_NAV`. **Crenup** = CReate+ENrich+UPdate, run by the Enricher (Create **1–5**, Enrich **1–10**; 10 = Embedding). One box in that order: Create · Enrich · List · Unlist · Delete — Update is the last three, never a button. Delete archives, reversible only by hand. Active/Verified/Partnered write from web-business (claim → verify → own → Stripe) — never admin. Admin states: General · Partnership · Crenup. Search eligibility: Discovery › Map; Places: one query/line. **Verification** = place ownership; **Ojo** = guest screenshot. Memo powers Discovery chat; Chat writes `discovery_config.chat.prompt`. **Models** (`/models-config`) = platform `models_config` picks + Enricher text/image/search quality tiers (`atlas_*`); Crenup shows those values read-only with a link.
- **Discovery** (`/filters-config`): nouns are **Mode** · **Source**. Three subpages: **Matrix** (`/matrix`, matrix + Signals; the index) · **Modes** (`/modes`): Word · Map · Catalog · Swipe · Chat · Favorites. Word is one mode, two passes; alone it returns Locations. **Sources** (`/sources`): nine: 3 Google · 4 Mesita Places · 2 Mesita Socials (last four Soon). Perplexity is no Source. Signals: eight in `_shared/discovery-signals.ts` (§8.3 order); `semantic` → Summary. Slice Save. Floor inside each box; types strip is a header, not a box. One owner per key, rest mirror: Autocomplete `general`, Nearby `map` floors, Mesita Nearby `filters` (also Home rails, Pay, bbox, Swipe). Soon gets no field. `requireReady`/`maxDistanceKm` unknobbed.
- **Visits** (`/visits-config`): Bill · Sync · Report (`visits_config`) plus **Visits Rewards** (`promos_config`, own Save; `/rewards-config` redirects) plus **Ojo** (`ojo_config`, own Save). Unwired visits keys stay off the HTML. **Orders Config** = the REMOTE context; its rates stay parked. Twin: `web-business/lib/business/promos.ts`. Per-place editor lives in `web-business` (`/places/[id]`); admin bulk only (`/manage-multiple`). **"promo"/"membership" BANNED.** **Payments** (`/controls-config`, `controls_config`): box **Credits** — the Wallet's hold (**3h**) + bonus + expiry, FALLBACKS a place may override, capped. Gifting Soon. Premium clamps up to the Free quota; the preselected tip must be a chip that exists.
- `database.types.ts` is hand-copied across web apps — regenerate from cloud, never hand-edit. CI `web-admin.yml`: lint · typecheck · test · build, Node 22+, path-filtered.
